class Founds {
  final String typeItem, colorOrmodel, wherefound, datefound, foundby, deliverymethod;
  Founds({ this.typeItem, this.colorOrmodel, this.wherefound, this.datefound,this.foundby,this.deliverymethod,
  });
  
}
